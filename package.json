{
  "name": "pi-gear",
  "version": "0.1.0",
  "description": "Pi coding-agent extension with macOS-sandboxed Bash, filesystem and network policy, task planning, subagents, and LSP tools.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "pi-coding-agent",
    "coding-agent",
    "ai-agent",
    "macos-sandbox",
    "filesystem-policy",
    "network-policy",
    "task-planning",
    "subagents",
    "language-server-protocol",
    "lsp"
  ],
  "packageManager": "bun@1.3.13",
  "license": "MIT",
  "type": "module",
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --test commands/*.test.ts config/*.test.ts context/*/*.test.ts execution/*/*.test.ts execution/*/*/*.test.ts lifecycle/*.test.ts lsp/*.test.ts subagents/*/*.test.ts subagents/*/*/*.test.ts ui/*/*.test.ts ui/*/*/*.test.ts",
    "test:sandbox:macos": "node --experimental-strip-types --test execution/sandbox/integration.test.ts && node --experimental-strip-types --test --test-name-pattern='sandbox spawn abort and timeout' execution/sandbox/spawn.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/sandbox-runtime": "0.0.71",
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.3",
    "@earendil-works/pi-tui": "0.84.3",
    "typebox": "1.3.13",
    "@types/node": "26.2.0",
    "typescript": "7.0.2"
  }
}
